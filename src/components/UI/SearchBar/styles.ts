import styled from 'styled-components';
import tw from 'tailwind.macro';

export const Container = styled.div`
    ${tw`flex w-full`}
`;

export const Input = styled.input`
    ${tw`rounded border-2 w-full pr-2 pl-2`}
`;